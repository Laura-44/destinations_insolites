<%= simple_form_for @destination do |f| %>
  <%= f.input :name, label: 'Nom de la destination' %>
  <%= f.input :description, label: 'Une petite description' %>
  <%# <%= f.input :date_of_availability, html5: true %>
  <%= f.input :price, label: 'Prix' %>
  <%= f.input :photos, as: :file, input_html: { multiple: true } %>
  <%= f.submit 'Création de la destination' %>
<% end %>
